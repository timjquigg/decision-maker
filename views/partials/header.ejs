<!--nav bar goes here -->
<nav>
  <form action="/polls" method="GET">
    <button class="home">
      <h2><i class="fa-solid fa-chart-simple"></i>Deisicion Maker</h2>
    </button>
  </form>
  <div class="nav-right">
    <% if (username !==undefined ) { %>
      <h3>Welcome back <%=username %>!</h3>
      </h3>
      <div class="buttons">
        <div class="createpoll">
          <form action="/polls/new" method="GET">
            <button class="createpoll">Create a new poll</button>
            <div class="icon"><i class="fa-regular fa-hand-pointer fa-beat-fade"></i></div>
          </form>
        </div>
        <form action="/users/logout" method="POST">
          <button class="logout">logout</button>
        </form>
      </div>
      <% } else { %>
        <h3>Welcome!</h3>
        <div class="buttons">
          <form action="/users" method="GET">
            <button>Login</button>
          </form>
        </div>
        <% } %>
</nav>